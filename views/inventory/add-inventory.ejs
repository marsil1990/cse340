<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } %>

        <%- messages() %>

            <section class="form-container">
                <form class="inventory-form" action="/inv/new-inventory" method="POST">
                    <label for="classificationList">Classification</label>
                    <%- classificationList %>
                        <label for="inv_make">Make</label>
                        <input id="inv_make" name="inv_make" required minlength="3"
                            pattern="^[A-Za-z0-9][A-Za-z0-9 \-]{2,}$"
                            title="Mínimo 3 caracteres. Solo letras, números, espacios y guiones."
                            value="<%= locals.inv_make %>" placeholder="Min of 3 Characters">

                        <label for="inv_model">Model</label>
                        <input id="inv_model" name="inv_model" required minlength="3"
                            pattern="^[A-Za-z0-9][A-Za-z0-9 \-]{2,}$"
                            title="Mínimo 3 caracteres. Solo letras, números, espacios y guiones."
                            value="<%= locals.inv_model %>" placeholder="Min of 3 Characters">

                        <label for="inv_description">Description</label>
                        <textarea id="inv_description" name="inv_description" required minlength="10"
                            placeholder="Brief description (min 10 chars)"
                            rows="4"><%=  locals.inv_description  %></textarea>

                        <label for="inv_image">Image Path</label>
                        <input id="inv_image" name="inv_image" required
                            pattern="^\/images\/vehicles\/.+\.(png|jpg|jpeg|webp)$"
                            title="The route must start with /images/vehicles/ and end in .png, .jpg, .jpeg o .webp"
                            value="<%= ( locals.inv_image !== undefined && locals.inv_image !== '') ? locals.inv_image : '/images/vehicles/no-image.png' %>"
                            placeholder="/images/vehicles/file.png">

                        <label for="inv_thumbnail">Thumbnail Path</label>
                        <input id="inv_thumbnail" name="inv_thumbnail" required
                            pattern="^\/images\/vehicles\/.+\.(png|jpg|jpeg|webp)$"
                            title="The root start with /images/vehicles/ and end in .png, .jpg, .jpeg o .webp"
                            value="<%= (locals.inv_thumbnail !== undefined && locals.inv_thumbnail !== '') ? locals.inv_thumbnail:'/images/vehicles/no-image.png' %>"
                            placeholder="/images/vehicles/file.png">

                        <label for="inv_price">Price</label>
                        <input id="inv_price" name="inv_price" required inputmode="decimal"
                            pattern="^\d+(?:\.\d{1,2})?$"
                            title="Número entero o decimal con punto (ej: 12000 o 12000.99)"
                            value="<%= locals.inv_price  %>" placeholder="Decimal or integer">

                        <label for="inv_year">Year</label>
                        <input id="inv_year" name="inv_year" required type="number" min="1950" max="2099"
                            title="Año de 4 dígitos entre 1950 y 2099 (ej: 2020)." value="<%= locals.inv_year  %>"
                            placeholder="4-digit year">

                        <label for="inv_miles">Miles</label>
                        <input id="inv_miles" name="inv_miles" required type="number" min="0" step="1"
                            title="Solo dígitos (ej: 65000)." value="<%= locals.inv_miles%>" placeholder="Digits only">

                        <label for="inv_color">Color</label>
                        <input id="inv_color" name="inv_color" required value="<%= locals.inv_color  %>">

                        <button type="submit">Add Vehicle</button>
                </form>
            </section>